# Keeps the Docker build context small and out of the image entirely --
# most importantly, never bakes local secrets or the job-store DB in.
.git/
.github/
.golangci.yml
Makefile
data/
*.db
*.db-shm
*.db-wal
.env
.env.*
!.env.example
.idea/
.vscode/
docs/
.agents/
.claude/
